"use client";

import { LegacyWorldBridge } from "@/components/legacy-world-bridge";

export default function CompendioPage() {
  return (
    <LegacyWorldBridge
      title="O compendio canonico agora e contextual."
      description="A base global antiga foi substituida por um compendio world-scoped. Esta ponte leva voce para o primeiro mundo ativo."
      targetLabel="compendio do mundo"
      segment="compendium"
    />
  );
}
